# Tasks: 002-deficiency-fixes

**Approach**: Fix 7 deficient implementations identified by systematic audit. Sequential execution — one task at a time. TDD — update/add tests first, verify they fail, then fix.

---

## Phase 1: Host Go Fixes

- [x] T009-fix Add struct tag validation to Config module. (1) Add `github.com/go-playground/validator/v10` to `go.mod`. (2) Add `validate:"..."` struct tags to all Config fields in `internal/config/config.go`: `Port` gets `validate:"required,min=1,max=65535"`, `TailscaleInterface` gets `validate:"required"`, `SignTimeout` gets `validate:"required,min=1"`, `ConnectionTimeout` gets `validate:"required,min=1"`, `SocketPath` gets `validate:"required"`, `ControlSocketPath` gets `validate:"required"`, `LogLevel` gets `validate:"required,oneof=debug info warn error fatal"`, `CertExpiry` gets `validate:"required"`. (3) Update `validate()` to run `validator.New().Struct(c)` first, collect tag validation errors, then run existing custom validation as a second pass. Both mechanisms must exist — struct tags AND custom validation. (4) Update tests in `internal/config/config_test.go` to verify: port=0 error contains `"min"`, port=70000 error contains `"max"`, empty tailscaleInterface error contains `"required"`, logLevel="trace" error contains `"oneof"`. [FR-300, FR-301, SC-102]
  **Done when**: `go test ./internal/config/...` passes. Test asserts error string contains `"min"` for port=0. Test asserts error string contains `"required"` for empty tailscaleInterface. Test asserts error string contains `"oneof"` for logLevel="trace". Both `validate:"..."` struct tags AND the custom `validate()` function exist in the code.

- [x] T010-fix Add logging to ShutdownManager. (1) Add `logger *slog.Logger` field to `ShutdownManager` struct. (2) Update `NewShutdownManager` to accept a `*slog.Logger` parameter. (3) In `Shutdown()`, log at INFO level with these exact `msg` values in this exact order: `"shutdown initiated"` before any work, `"stopping new connections"` before `stopFunc()`, `"draining in-flight requests"` before WaitGroup wait, `"executing shutdown hooks"` before hook loop, `"shutdown complete"` after all hooks. That is 5 log messages total — count them. (4) Add a `logFlush func()` field called as the very last step after `"shutdown complete"`. If `logFlush` is nil, skip (no-op). (5) Update all callers of `NewShutdownManager` (daemon.go, tests) to pass the logger. In daemon.go, pass a logFlush that syncs the slog handler. (6) Update tests: capture log output with a buffer-backed slog handler, assert exactly 5 INFO messages appear, assert first message `msg` is `"shutdown initiated"`, assert last message `msg` is `"shutdown complete"`, assert logFlush callback was invoked after `"shutdown complete"`. [FR-302, FR-303, FR-304, SC-103]
  **Done when**: `go test ./internal/daemon/...` passes. Test captures log output and finds exactly 5 messages with `msg` values `"shutdown initiated"`, `"stopping new connections"`, `"draining in-flight requests"`, `"executing shutdown hooks"`, `"shutdown complete"` in that order. logFlush callback invoked last when non-nil, skipped when nil.

- [ ] T044-fix Load Nix-declared devices from config.json. [consumes: IC-D01] (1) Add `DeviceConfig` struct to `internal/config/config.go` with fields and JSON tags that exactly match the NixOS module's JSON output (verified against `nix/module.nix` lines 116-125): `Name string` `json:"name"`, `TailscaleIP string` `json:"tailscaleIp"`, `Port int` `json:"port"`, `CertFingerprint string` `json:"certFingerprint"`, `ClientCertPath string` `json:"clientCertPath"`, `ClientKeyPath string` `json:"clientKeyPath"`. Use `*string` for ClientCertPath and ClientKeyPath since the NixOS module outputs null when not set. No validation struct tag needed on the `Devices` field. (2) Add `Devices map[string]DeviceConfig` field to Config struct with `json:"devices,omitempty"`. (3) In `cmd/nix-key/daemon.go` `runDaemon()`, convert `cfg.Devices` to `[]daemon.Device` slice and pass as first argument to `registry.Merge(nixDevices, runtimeDevices)` instead of `nil`. (4) Cross-boundary contract test: create a JSON string matching the exact format the NixOS module produces (with keys `name`, `tailscaleIp`, `port`, `certFingerprint`, `clientCertPath`, `clientKeyPath` — including null values for optional fields), unmarshal into Config, and assert all fields populated correctly. This verifies the Go struct's JSON tags match the Nix producer's output format. (5) Add integration test: daemon with config containing Nix devices + separate devices.json → both sources visible in registry. [FR-305, FR-306, FR-307, SC-100]
  **Done when**: `go test ./internal/config/... ./cmd/nix-key/...` passes. Contract test proves JSON `{"devices": {"myphone": {"name": "myphone", "tailscaleIp": "100.64.0.2", "port": 50051, "certFingerprint": "SHA256:abc", "clientCertPath": null, "clientKeyPath": null}}}` round-trips through Config struct with all fields correct and null pointers for optional fields. Integration test proves daemon loads devices from both config and devices.json.

- [ ] T054-fix Add STATUS column to `nix-key devices` output. [produces: IC-D02] (1) First, check whether the control socket `list-devices` response includes cert file paths and connection info (TailscaleIP, Port, client cert/key paths) needed for mTLS Ping — if not, extend the response to include them. (2) In `cmd/nix-key/devices.go`, after fetching device list, perform concurrent Ping RPC to each device with 2s timeout per device (reuse mTLS dial + Ping logic from `cmd/nix-key/test.go`). Use `sync.WaitGroup` + goroutines. (3) Change the table header to exactly: `"NAME\tTAILSCALE IP\tCERT FINGERPRINT\tLAST SEEN\tSTATUS\tSOURCE"` — the word `STATUS` must appear as a column header, not `STATE`, `CONNECTIVITY`, or any other synonym. (4) STATUS cell values are exactly `"online"` (Ping succeeds), `"offline"` (Ping fails, times out, or any connection error), `"unknown"` (no cert paths available to attempt mTLS). No other values. (5) The existing SOURCE column (`"nix-declared"` / `"runtime-paired"`) remains — both columns present. (6) Update integration test in `cmd/nix-key/integration_test.go`: assert output contains the string `"STATUS"` as a column header, assert a reachable device row contains `"online"`, assert an unreachable device row contains `"offline"`. [FR-308, FR-309, FR-310, SC-101]
  **Done when**: `go test ./cmd/nix-key/...` passes. Test asserts the table header contains the exact string `"STATUS"`. Test asserts a device row contains `"online"` or `"offline"`. Test asserts `"SOURCE"` column still present alongside `"STATUS"`.

## Phase 2: Script Fix

- [ ] T072-fix Fix `scripts/verify-release-pipeline.sh` grep patterns. (1) Read the current `scripts/verify-release-pipeline.sh` and `.github/workflows/e2e.yml` to identify all mismatched grep patterns. (2) In Section 4 (E2E triggers): replace the grep checking for `push:` with a grep checking for `workflow_run:`. Replace the grep checking for `pull_request:` with a grep checking for `workflow_call:`. (3) Update section comments/descriptions to say "workflow_run trigger" not "push trigger". (4) Check all other sections for similar mismatches against the current workflow YAML files — fix any found. (5) Run the script end-to-end and verify exit code 0. [FR-311, SC-105]
  **Done when**: `bash scripts/verify-release-pipeline.sh` exits 0 against the current workflow files. No false failures. Every grep pattern in the script matches the actual content of the YAML file it checks.

## Phase 3: Android Fixes

- [ ] T034-fix Implement real TailscaleBackend. [produces: IC-D04] (1) Create new package `pkg/tsbridge/tailscale.go` with a `TailscaleService` struct wrapping `tsnet.Server` (same library `test/phonesim/main.go` uses — reference its import of `tailscale.com/tsnet`). Methods: `Start(authKey, dataDir string) (string, error)` — starts tsnet.Server, authenticates, returns OAuth URL or empty string; `Stop() error` — calls `tsnet.Server.Close()`; `GetIP() (string, error)` — returns `tsnet.Server.TailscaleIPs()` first IPv4; `IsRunning() bool`. All methods exported for gomobile. (2) In `android/app/src/main/java/com/nixkey/tailscale/`, create `RealTailscaleBackend.kt` implementing `TailscaleBackend` interface by delegating to the gomobile-generated `TailscaleService` class. gomobile maps Go `(string, error)` to Kotlin `String?` + exception, which matches the existing interface `fun start(authKey: String?, dataDir: String): String?`. (3) In `AppModule.kt`, replace the anonymous stub object with `RealTailscaleBackend(TailscaleService())`. (4) Move the old stub code to `FakeTailscaleBackend.kt` in `androidTest/` for test use. (5) Update `make gomobile` in `Makefile` to include `pkg/tsbridge/` in the gomobile bind command alongside `pkg/phoneserver/`. Rebuild AAR. (6) Write instrumented test using `FakeTailscaleBackend` verifying the `TailscaleBackend` interface contract (start returns string or throws, getIp returns string, stop doesn't throw, isRunning reflects state). (7) Verify the built AAR contains the TailscaleService class: `jar tf android/app/libs/phoneserver.aar | grep -i tailscale`. [FR-315, FR-316, FR-317, FR-318, SC-106]
  **Done when**: `make gomobile` succeeds. `jar tf android/app/libs/phoneserver.aar` output contains `TailscaleService` class. `RealTailscaleBackend.kt` compiles against the AAR. `AppModule.kt` provides `RealTailscaleBackend` (not stub). Production code in `AppModule.kt` contains zero hardcoded IP strings (no `"100.100.100.100"`). Instrumented tests pass with `FakeTailscaleBackend`. SC-106 verified manually (real Tailnet requires auth key not available in CI).

- [ ] T089-fix Add loading states to exactly these 3 screens: `ServerListScreen`, `KeyListScreen`, `KeyDetailScreen`. (Not `PairingScreen` or `TailscaleAuthScreen` — those already have loading states.) (1) In `ServerListViewModel.kt`: add `private val _isLoading = MutableStateFlow(true)` and `val isLoading: StateFlow<Boolean> = _isLoading.asStateFlow()`. Set `_isLoading.value = false` after host data fetch completes (success or error). (2) In `KeyListViewModel.kt`: same pattern — `isLoading` starts true, set false after key list fetch. (3) In `KeyDetailViewModel.kt`: same pattern — `isLoading` starts true, set false after key detail fetch. (4) In `ServerListScreen.kt` composable: `val isLoading by viewModel.isLoading.collectAsState()`. When true: `Box(modifier = Modifier.fillMaxSize(), contentAlignment = Alignment.Center) { CircularProgressIndicator() }`. When false: existing content. (5) Same pattern in `KeyListScreen.kt` and `KeyDetailScreen.kt`. (6) Add UI tests: for each of the 3 screens, verify `CircularProgressIndicator` is displayed. Use Compose testing rule with `onNodeWithTag("loading")` or semantic matcher. [FR-312, FR-313, FR-314, SC-104]
  **Done when**: `./gradlew testDebugUnitTest` passes. `ServerListScreen.kt` contains `CircularProgressIndicator`. `KeyListScreen.kt` contains `CircularProgressIndicator`. `KeyDetailScreen.kt` contains `CircularProgressIndicator`. All 3 ViewModels expose `isLoading: StateFlow<Boolean>`. Existing Android tests still pass.

## Validation

- [ ] T-validate Run full validation after all fixes: (1) `make validate` (test + lint + security-scan) — must exit 0. (2) `nix flake check` — must exit 0. (3) `cd android && ./gradlew assembleDebug testDebugUnitTest` — must exit 0. (4) Verify test counts have not decreased vs baseline. (5) Verify no production code contains the hardcoded string `"100.100.100.100"`. [SC-107]
  **Done when**: All three validation commands exit 0. No regressions. `grep -r "100.100.100.100" internal/ pkg/ cmd/ android/app/src/main/` returns zero matches (stub fully removed from production code).

---

## Execution Order

```
T009-fix → T010-fix → T044-fix → T054-fix → T072-fix → T034-fix → T089-fix → T-validate
```

Strictly sequential. Each task completes before the next begins.
