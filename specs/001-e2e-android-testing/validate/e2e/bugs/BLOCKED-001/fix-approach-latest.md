BLOCKED-001 is not a standalone bug — it is a cascading failure caused by BUG-010, BUG-011, and BUG-012. No dedicated fix is needed. With all three upstream bugs fixed, the full navigation path (TailscaleAuth → ServerList → KeyManagement) becomes reachable: headscale auth keys pass validation, the pair-info-file produces correctly formatted payloads, and pairing proceeds without requiring a real Tailscale IP.
