Replaced the direct `json.Marshal(qrParams)` call in `pair.go:170` with `GenerateQRPayload(qrParams)`, which already exists and produces the correct base64-encoded JSON with lowercase field names and the required `v:1` version field. This ensures the `--pair-info-file` output is identical to the QR code payload, which the phone's `decodeQrPayload()` expects. Removed the now-unused `encoding/json` import.
