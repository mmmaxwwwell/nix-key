Updated the auth key validation regex in `TailscaleAuthViewModel.kt:192` from `^tskey-(auth-)?[a-zA-Z0-9-]+$` to `^(tskey|hskey)-(auth-)?[a-zA-Z0-9_-]+$`. This adds support for the headscale `hskey-` prefix and includes underscore in the character class, which headscale keys use in their random portion. Also updated the placeholder text in `TailscaleAuthScreen.kt:133` to hint that both `tskey-auth-...` and `hskey-auth-...` formats are accepted.
